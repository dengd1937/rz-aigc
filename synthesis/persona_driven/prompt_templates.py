"""
Prompt 模板定义
基于 persona_driven 数据生成方法
"""

# 指令生成模板
instruction_following = """请为以下人物角色创建一个可验证的指令：

{persona}

可验证指令的示例：{example}

**关键要求**：
1. **分类锚定**：指令内容必须严格围绕分类：{category}，确保内容与分类定义高度相关
2. **约束包含**：指令必须包含以下所有可验证约束：{constraints}
3. **输出格式**：你的输出应以"用户指令："开头，不应包含对指令的回答
4. **内容聚焦**：避免泛化描述，确保生成的指令具有明确的{category}领域特征
"""

# 其他分类专用指令生成模板 - 避免生成涉及已定义分类的内容
instruction_following_other = """请为以下人物角色创建一个可验证的指令：

{persona}

可验证指令的示例：{example}

**关键要求**：
1. **分类锚定**：这个角色属于"其他分类"，指令内容必须严格避免涉及以下20个分类：
   - 文学小说、青春文学、亲子育儿、科普读物、动漫幽默、人文社科
   - 艺术收藏、古籍地理、旅游休闲、经济管理、励志成长、外语学习
   - 法律哲学、政治军事、自然科学、家庭教育、两性关系、孕产育儿
   - 家居生活、生活时尚

2. **内容选择**：创建与上述分类都无关的内容，例如：
   - 体育竞技（训练方法、比赛分析、运动装备评测）
   - 游戏攻略（游戏技巧、关卡指南、角色分析）
   - 纯娱乐八卦（明星轶事、综艺点评）
   - 技术手册（软件使用、设备操作指南）
   - 其他明确不属于上述20个分类的领域

3. **约束包含**：指令必须包含以下所有可验证约束：{constraints}
4. **输出格式**：你的输出应以"用户指令："开头，不应包含对指令的回答
"""

# 响应生成模板
instruction_following_solution = """请提供满足约束条件的指令响应。

指令：{instruction}

【必须严格遵守的约束】：
1. **字数限制**：响应总字数必须严格控制在500字以内（包含标点符号）！
2. **完整性**：必须以中文句号（。）、感叹号（！）或问号（？）结尾，不能截断！
3. **格式要求**：不要在响应前后添加任何引号、代码块标记或其他格式字符！
4. **内容要求**：遵循指令并精确满足所有约束条件
5. **精简表达**：如果内容较长，请精简表达，保留核心信息

【重要提示】：
- 请仔细估算字数，确保不超过500字
- 如果内容较多，优先保留核心要点，删除冗余描述
- 直接输出响应内容，不要添加任何额外说明、引号或格式标记"""

# 重写模板（用于生成拒绝响应）
rewrite_if_prompt = """重写给定的指令，移除其中一个约束。

指令：{instruction}

注意：
1. 你应该在保持连贯性的同时重写指令，并放松以下约束类别之一：{constraints}
2. 记住要完全放松以下约束类别之一：{category}
3. 你的输出应以"用户指令："开头。你的输出不应包含对指令的回答。
"""
